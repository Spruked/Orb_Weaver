# ORB Desktop MCP — Dependencies
# Core (required)
pyautogui>=0.9.54
Pillow>=10.0.0
pyperclip>=1.8.2

# Browser control (optional — needed for orb_browser_* tools)
playwright>=1.40.0

# Local LLM detection (optional — needed for use_llm=True)
requests>=2.31.0

# Dev / test
pytest>=7.4.0
pytest-asyncio>=0.23.0
